apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/component: test
  name: test
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: http
  selector:
    app.kubernetes.io/component: test
  type: ClusterIP
